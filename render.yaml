# Render.com Configuration
# This file helps Render automatically detect your build settings

# Build settings
build:
  - cd backend && mvn clean install -DskipTests

# Runtime
runtime: java

# Start command  
start: cd backend && java -jar target/*.jar

# Health check
healthCheckPath: /actuator/health
