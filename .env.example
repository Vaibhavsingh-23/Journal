# Environment Variables Template for Render Deployment

# Copy these to Render's Environment Variables section

# MongoDB Configuration
SPRING_DATA_MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/journal_db

# Gemini API Configuration  
GEMINI_API_KEY=your_gemini_api_key_here

# Server Configuration (Render uses port 10000 by default)
SERVER_PORT=10000

# Email Configuration (Optional - for weekly summaries)
SPRING_MAIL_HOST=smtp.gmail.com
SPRING_MAIL_PORT=587
SPRING_MAIL_USERNAME=your_email@gmail.com
SPRING_MAIL_PASSWORD=your_app_password

# Instructions:
# 1. Go to Railway dashboard > Your service > Variables tab
# 2. Add each variable above with your actual values
# 3. For MongoDB: Use your MongoDB Atlas connection string
# 4. For Gemini: Get API key from https://makersuite.google.com/app/apikey
# 5. For Email: Use Gmail app password (not your regular password)
